# Engine environment variables — copy to .env and fill in.

# Server
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=info

# HMAC secret for signing callbacks to Next.js app (must match ENGINE_HMAC_SECRET in web app)
ENGINE_HMAC_SECRET=generate-a-random-32-byte-hex-string

# Sentry (optional)
SENTRY_DSN=
