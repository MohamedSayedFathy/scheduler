/**
 * @scheduler/types — Shared type definitions.
 *
 * This package contains types used by both the Next.js web app and the
 * Python scheduling engine (via codegen or manual sync).
 */

export * from './domain';
export * from './engine';
